rurep a grep like tool written in rust.

How to use it:-

rurep "pattern" "file"

rurep -n "pattern" "file" -- (to get line number also)

rurep -i "Pattern" "file" -- (to search for pattern by ignoring case )
